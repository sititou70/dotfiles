#!/bin/bash
set -euo pipefail

# 1. 差分から日本語のメッセージを生成して変数に格納
MSG=$(git diff --cached | claude -p "以下のdiffに対し、日本語のコミットメッセージを作成し、本文のみ出力してください。構成は1行目に要約、2行目は空行、3行目以降に詳細な変更点としてください。以下はdiffの内容です:")

# 2. 変数の内容をメッセージとして渡しつつ、エディタを起動
git commit -m "$MSG" -e
